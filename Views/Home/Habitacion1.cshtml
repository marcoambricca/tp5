<div class="container p-5 my-5 bg-dark text-white">
    <span class="reloj">@ViewBag.minutos : @ViewBag.segundos</span><br><br>
    <img src="/img/acertijo1.jpg" alt="acertijo1" class="img-thumbnail">
    <span class="ms-5">¿Que numero hay debajo del auto?</span>
    <form class="mt-5"action='@Url.Action("Habitacion", "Home")'>
        <input type="hidden" name="sala" value="1">
        <p>Escribi tu respuesta @ViewBag.nombre</p>
        <input type="text" name="clave" value="">
        <input type="submit" class="btn btn-outline-primary">
    </form>
    <button data-bs-toggle="collapse" data-bs-target="#pista">Pista</button>
    <div id="pista" class="collapse">Da vuelta la pantalla.</div>
    @{
        if (ViewBag.MensajeError != null)
        {
            <br>
            <br>
            <div class="alert alert-danger alert-dismissible">
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                <strong>Error!</strong> Respuesta incorrecta.
            </div>
        }
    }

</div>